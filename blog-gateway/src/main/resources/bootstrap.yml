server:
  port: 6001

spring:
  application:
    name: gateway-server
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 #注册中心地址
    gateway:
      discovery:
        locator:
          #使用服务名称访问服务
          enabled: true
